# Web / API
fastapi
uvicorn[standard]
jinja2
python-multipart

# Data / validation
pydantic>=2
email-validator
numpy

# zoneinfo (stdlib) has no IANA tz database on Windows by default; harmless
# no-op on Linux/macOS where the OS already provides one.
tzdata; sys_platform == "win32"

# Google (Gmail + Google Calendar)
google-auth
google-auth-oauthlib
google-auth-httplib2
google-api-python-client

# Local LLM runtime (verified working: foundry-local-sdk 1.2.4, see CLAUDE.md)
foundry-local-sdk

# Optional cloud LLM backend (LLM_PROVIDER=gemini in .env, see CLAUDE.md) —
# a deliberate opt-in departure from the local/offline default.
google-genai

# Config
python-dotenv

# Test
pytest
pytest-mock
httpx

# Dev tools (optional)
pyflakes
